{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist-electron/**"],
    },
    "dev": {
      "dependsOn": ["@forgetools/server#build"],
      "persistent": true,
    },
    "start": {
      "dependsOn": ["build", "@forgetools/web#build", "@forgetools/server#build"],
      "cache": false,
      "persistent": true,
    },
    "smoke-test": {
      "dependsOn": ["build", "@forgetools/web#build", "@forgetools/server#build"],
      "cache": false,
      "outputs": [],
    },
  },
}
